var anyType;
//타입스크립트는 타입에 관한 더 많은 정보를 제시할수록 좋다.
var price;
price: 3;
price: 'goldmedal';
var totalCost;
var orderID;
var calculateTotalCost = function (price, qty) {
};
var findOrderID = function (customer, productId) {
    return orderID;
};
var setItemPrice = function (variable) {
    if (typeof variable === 'string') {
        totalCost = 0;
    }
    else {
        totalCost = variable;
    }
};
setItemPrice(50);
